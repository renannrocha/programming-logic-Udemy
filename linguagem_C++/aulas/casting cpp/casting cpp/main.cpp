#include <bits/stdc++.h>

using namespace std;

int main()
{
    // casting em c++

    double b1, b2, h, area;
    b1 = 6.0;
    b2 = 8.0;
    h = 5.0;
    area = (b1 + b2) / 2.0 * h;
    cout << fixed << setprecision(2);
    cout << area << endl;

    return 0;
}
